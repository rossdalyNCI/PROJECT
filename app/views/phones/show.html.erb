<div class="page-header">
  <h1><a href="#"><%= @phone.make %></a><br> <small>You deserve this phone</small></h1>
  <p>
  <strong>Price:</strong>
  <%= @phone.price %>
</p>
</div>

<div class="btn-group">
	<%= link_to 'Back', root_path, class: "btn btn-primary" %>
</div>

	<div class="btn-group">
		<%= link_to 'Edit', edit_phone_path(@phone), class: "btn btn-default" %>
		<%= link_to 'Destroy', @phone, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-default" %>
	</div>
